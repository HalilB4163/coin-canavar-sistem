name: Coin_Canavar_Otomatiği

on:
  schedule:
    - cron: "*/15 * * * *" # Her 15 dakikada bir otomatik çalışır
  workflow_dispatch: # Manuel çalıştırma butonu

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Dosya yazma izni
    steps:
      - name: 1. Dosyaları Çek
        uses: actions/checkout@v3

      - name: 2. Python Hazırla
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 3. Kütüphaneleri Yükle
        run: pip install requests pandas numpy

      - name: 4. Analizi Başlat
        run: python coin_backend/analyzer.py

      - name: 5. Veriyi Kaydet ve Push Yap
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          
          # Dosyayı ekle
          git add coin_backend/data.json
          
          # Değişiklik kontrolü ve commit
          git commit -m "📊 Veri Güncellendi: $(date)" || echo "Değişiklik yok"
          
          # Zorunlu push
          git push origin main
